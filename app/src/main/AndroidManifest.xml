<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".ZhiApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZHITool">

        <!-- LSPosed 模块声明：小稚的 Xiaomi 17 系列背屏工具 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/module_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/module_scope" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <!-- MIUI 背屏准入证 -->
        <meta-data
            android:name="miui.rear.policy"
            android:value="1" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ZHITool"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- 背屏歌词显示 Activity（由 root 投放到副屏 display 1） -->
        <activity
            android:name=".rear.RearLyricActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity="com.zhitool.rearlyric.rear"
            android:theme="@style/Theme.ZHITool.RearTransparent"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode|smallestScreenSize|density"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:hardwareAccelerated="true">
            <meta-data
                android:name="miui.rear.policy"
                android:value="miui" />
        </activity>

        <!-- 歌词订阅前台服务 -->
        <service
            android:name=".lyric.LyricService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="背屏歌词显示" />
        </service>

        <!-- 开机后恢复歌词订阅与自动投放；Root 初始化由 LyricService 自身在后台完成。 -->
        <receiver
            android:name=".lyric.LyricBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 背屏工具保活/常亮/遮盖检测前台服务 -->
        <service
            android:name=".tools.RearToolsService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="背屏常亮与保活" />
        </service>

        <!-- 控制中心快捷开关：切换至背屏 -->
        <service
            android:name=".tools.SwitchToRearTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_switch"
            android:label="@string/tile_switch"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 控制中心快捷开关：背屏截图 -->
        <service
            android:name=".tools.RearScreenshotTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_screenshot"
            android:label="@string/tile_screenshot"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 控制中心快捷开关：背屏录屏 -->
        <service
            android:name=".tools.RearScreenRecordTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_record"
            android:label="@string/tile_record"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 背屏录屏前台服务（悬浮窗 + screenrecord 纯视频） -->
        <service
            android:name=".tools.record.ScreenRecordService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="背屏录屏" />
        </service>

        <!-- 充电动画背屏页（插/拔电监听在 RearToolsService 运行时注册，manifest 接收器在 Android 8+ 不送达） -->
        <activity
            android:name=".tools.charge.RearChargingActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity="com.zhitool.rearlyric.charge"
            android:theme="@style/Theme.ZHITool.RearTransparent"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode|smallestScreenSize|density"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:hardwareAccelerated="true">
            <meta-data
                android:name="miui.rear.policy"
                android:value="miui" />
        </activity>

        <!-- 背屏通知推送：由 LSPosed/SystemUI 捕获后显式发送；不申请通知使用权。 -->
        <receiver
            android:name=".tools.notify.NotificationBridgeReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.STATUS_BAR_SERVICE">
            <intent-filter>
                <action android:name="com.zhitool.rearlyric.action.NOTIFICATION_POSTED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".tools.notify.RearNotificationActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity="com.zhitool.rearlyric.notify"
            android:theme="@style/Theme.ZHITool.RearTransparent"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode|smallestScreenSize|density"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:hardwareAccelerated="true">
            <meta-data
                android:name="miui.rear.policy"
                android:value="miui" />
        </activity>

        <!-- URI 调用（zhitool:// 与兼容 mrss://），透明、即收即走 -->
        <activity
            android:name=".tools.UriReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:noHistory="true"
            android:taskAffinity="com.zhitool.rearlyric.uri"
            android:theme="@style/Theme.ZHITool.RearTransparent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zhitool" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mrss" />
            </intent-filter>
        </activity>

    </application>
</manifest>
